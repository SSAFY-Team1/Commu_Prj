# LocalHub 의뢰서 중심 비교 분석 검토

## 1. 검토 목적

사용자가 지적한 것처럼 개발 의뢰서의 핵심 요청은 "공통 레이아웃을 반드시 설계하라"가 아니다. 의뢰서의 요청 내용은 제공된 5개 권역 공공데이터 중 1개 권역을 선정하고, Vue.js 3 기반 정적 SPA로 지역 정보 공유 익명 커뮤니티 플랫폼 `LocalHub`를 구축하는 것이다.

따라서 본 문서는 기존에 작성한 화면/기능 요구사항 문서에서 과하게 잡힌 부분을 조정하고, 현재 프로젝트 코드와 최종 실습기획서가 의뢰서 핵심 요구를 잘 반영하고 있는지 비교 분석한 검토 문서이다.

본 검토에서는 코드와 원본 기획서 파일을 수정하지 않았다. 새 검토 문서만 별도 산출물로 작성한다.

## 2. 의뢰서 요청 내용의 핵심 재정리

의뢰서의 요청 내용은 다음 7개 축으로 요약할 수 있다.

| 축 | 의뢰서 기준 해석 | 필수 여부 |
|---|---|---|
| 권역 선택 | 전국 5개 권역 중 제공 JSON 데이터 1곳을 선정 | 필수 |
| 데이터 활용 | 제공 JSON을 프론트엔드에서 직접 불러와 처리 | 필수 |
| 앱 구조 | Vue.js 3, Vite 기반 정적 SPA | 필수 |
| 서버 구조 | 별도 상시 백엔드 서버 없음 | 필수 |
| 커뮤니티 | 지역 주민/관광객이 정보를 공유·소비하는 익명 커뮤니티 | 필수 |
| 추가 데이터 | 추가 수집 시 라이선스·공공누리 유형 확인 | 조건부 필수 |
| 제출 | 납기 내 결과물, 발표자료, 지정 산출물 SSAFY GitLab 제출 | 필수 |

즉, 공통 Header, Footer, 상세한 공통 레이아웃 체계는 의뢰서의 직접 필수 항목이 아니다. 다만 SPA를 구성하고 사용자가 화면을 이동하려면 최소한의 내비게이션과 화면 구조는 필요하므로, 이는 "필수 요구사항"이 아니라 "구현 편의 및 발표 품질을 위한 보조 설계"로 보는 것이 정확하다.

## 3. 공통 레이아웃에 대한 판단

### 결론

공통 레이아웃은 반드시 별도 요구사항으로 크게 작성할 필요는 없다.

다만 현재 프로젝트처럼 `Header`, `Footer`, `router-view`, `Chatbot`을 공통으로 배치하는 구조는 Vue SPA 구현상 자연스러운 선택이며, 의뢰서 위반은 아니다. 문제는 문서에서 이를 필수 요구사항처럼 강조하면 실제 의뢰 범위보다 커 보일 수 있다는 점이다.

### 적정 표현

기존 표현:

> 모든 주요 화면은 동일한 상단 헤더를 사용한다.

조정 권장 표현:

> 화면 이동과 발표 시연을 위해 최소 공통 구조를 둔다. 단, 의뢰서의 필수 구현 범위는 공통 레이아웃 자체가 아니라 지역 정보 조회, 익명 커뮤니티, 챗봇, 배포 산출물이다.

## 4. 현재 프로젝트 반영 검토

### 4.1 권역 선택 및 데이터 활용

| 검토 항목 | 현재 상태 | 판단 |
|---|---|---|
| 1개 권역 선정 | `서울` 권역으로 고정 | 반영 |
| 제공 JSON 사용 | `public/data/manifest.json`과 서울 JSON 7개 사용 | 반영 |
| 프론트엔드 직접 로딩 | `src/utils/dataLoader.js`에서 `/data` 하위 JSON fetch | 반영 |
| 데이터 정규화 | `normalizeItem()`으로 공통 필드 변환 | 반영 |
| 추가 데이터 라이선스 | 별도 추가 데이터 사용 정황 없음 | 문제 없음 |

근거:

- `public/data/manifest.json`에 서울 관광지, 문화시설, 축제공연행사, 여행코스, 레포츠, 숙박, 쇼핑 데이터가 등록되어 있다.
- `src/utils/dataLoader.js` 45-50행에서 JSON을 fetch하고, 108-127행에서 manifest 기반 전체 데이터를 로딩한다.
- `src/utils/dataLoader.js` 62-87행에서 TourAPI 필드를 화면용 공통 구조로 정규화한다.

검토 메모:

- 현재 제공 파일 합산은 6,518건이다.
- 최종 기획서와 `서울/SOURCE.md`에 8,150건 언급이 있으므로, 발표자료에서는 "현재 프로젝트에 포함된 7개 JSON 기준 6,518건"이라고 말하는 것이 안전하다.
- 음식점 데이터는 현재 manifest에 없다. 의뢰서 예시에는 맛집/음식점 활용이 가능하지만, 현재 프로젝트 기준 필수 누락으로 보기는 어렵다. 제공 데이터 범위가 현재 7개 카테고리이기 때문이다.

### 4.2 Vue 3 + Vite 정적 SPA

| 검토 항목 | 현재 상태 | 판단 |
|---|---|---|
| Vue 3 사용 | `package.json`에 Vue 3 의존성 | 반영 |
| Vite 사용 | `vite`, `@vitejs/plugin-vue` 사용 | 반영 |
| SPA 라우팅 | `vue-router`로 `/`, `/category/:id`, `/community`, `/dashboard` 구성 | 반영 |
| 정적 배포 가능 | `npm run build` 성공 | 반영 |

근거:

- `src/router/index.js` 7-11행에 주요 라우트가 정의되어 있다.
- `netlify.toml` 1-4행에 빌드 명령, 배포 디렉터리, Functions 디렉터리가 설정되어 있다.
- `netlify.toml` 6-9행에 SPA 새로고침 대응 redirect가 있다.

검증 결과:

- `npm run test -- --run`: 통과
- `npm run build`: 통과

### 4.3 익명 커뮤니티

| 검토 항목 | 현재 상태 | 판단 |
|---|---|---|
| 회원가입/로그인 없음 | 별도 인증 없음 | 반영 |
| 게시글 작성 | 제목, 비밀번호, 내용 입력 가능 | 반영 |
| localStorage 저장 | `src/utils/localStorage.js` 사용 | 반영 |
| 목록 조회 | 같은 화면에서 게시글 목록 표시 | 반영 |
| 수정 | 비밀번호 확인 후 수정 | 반영 |
| 삭제 | `prompt()`로 비밀번호 확인 후 삭제 | 반영 |
| 의뢰서 와이어프레임처럼 목록/상세/작성 분리 | 한 화면에 작성·목록·본문·수정이 섞여 있음 | 부분 반영 |
| 게시글 검색/페이지네이션 | 현재 커뮤니티 화면에는 없음 | 선택/미반영 |

근거:

- `src/views/CommunityView.vue` 5-10행에서 작성 폼을 제공한다.
- `src/views/CommunityView.vue` 12-22행에서 게시글을 반복 출력한다.
- `src/views/CommunityView.vue` 60-71행에서 게시글 생성 후 저장한다.
- `src/views/CommunityView.vue` 81-89행에서 수정 비밀번호를 검증한다.
- `src/views/CommunityView.vue` 97-102행에서 삭제 비밀번호를 검증한다.
- `src/utils/localStorage.js` 19-30행에서 게시글을 localStorage에 읽고 저장한다.

검토 메모:

- 기능 관점에서는 CRUD 핵심이 반영되어 있다.
- 화면 구성 관점에서는 의뢰서 참고 이미지의 "목록 화면", "상세 화면", "작성/수정 화면" 분리와는 다르다.
- 하지만 사용자가 말한 의뢰서 요청 내용만 기준으로 보면, 화면 분리는 핵심 요청이 아니라 참고 와이어프레임이다.
- 발표/평가에서 와이어프레임 일치도를 중시한다면 게시판 화면 분리는 개선 대상이지만, 기능 필수 조건만 보면 현재도 MVP로 설명 가능하다.

### 4.4 챗봇 구조

| 검토 항목 | 현재 상태 | 판단 |
|---|---|---|
| 플로팅 챗봇 버튼 | 우측 하단 고정 버튼 | 반영 |
| 대화창 UI | 메시지 목록, 입력창, 전송 버튼 | 반영 |
| 제공 데이터 기반 context | 검색 결과 최대 5건 전달 | 반영 |
| Netlify Function 호출 | `/.netlify/functions/chat` 호출 | 반영 |
| 프론트 API 키 노출 방지 | `VITE_OPENAI_API_KEY` 사용 없음 | 반영 |
| `OPENAI_API_KEY` 서버 환경변수 | Function에서 `process.env.OPENAI_API_KEY` 사용 | 반영 |
| 오류 처리 | 프론트와 Function 양쪽에 기본 오류 처리 | 반영 |

근거:

- `src/components/Chatbot.vue` 3-10행에 플로팅 버튼이 있다.
- `src/components/Chatbot.vue` 12-53행에 챗봇 대화창이 있다.
- `src/components/Chatbot.vue` 77-83행에서 데이터 검색 후 챗봇 요청을 보낸다.
- `src/services/chatApi.js`에서 `/.netlify/functions/chat`을 호출한다.
- `netlify/functions/chat.mjs` 38-56행에서 POST, JSON, 질문 길이를 검증한다.
- `netlify/functions/chat.mjs` 59-63행에서 `OPENAI_API_KEY`가 없을 때 로컬 스텁 응답을 반환한다.
- `netlify/functions/chat.mjs` 78-88행에서 OpenAI Responses API를 호출한다.

중요 판단:

- 의뢰서 원문에는 프론트엔드 직접 호출 예시가 있지만, 최종 실습기획서에서 Netlify Functions 구조로 보완한 것은 타당하다.
- "별도 백엔드 서버 없음"과 충돌해 보일 수 있으나, Netlify Function은 상시 백엔드 서버나 DB가 아니라 API 키 보호용 서버리스 중계 함수로 설명하면 된다.
- 발표에서는 "의뢰서의 정적 SPA 원칙은 유지하되, API 키 노출 방지를 위해 Netlify 배포 환경에 맞춘 보안 보완을 적용했다"고 말하는 것이 좋다.

### 4.5 선택 기능

| 검토 항목 | 현재 상태 | 판단 |
|---|---|---|
| 선택 기능 1개 이상 | 데이터 시각화 대시보드 구현 | 반영 |
| Chart.js 사용 | `chart.js/auto` 사용 | 반영 |
| KPI | 전체 콘텐츠, 카테고리 수, 게시글 수 표시 | 반영 |
| 차트 | 카테고리 막대, 비율 도넛, 자치구 Top 10 | 반영 |

근거:

- `src/views/DashboardView.vue` 9-13행에서 KPI를 표시한다.
- `src/views/DashboardView.vue` 17-38행에서 차트 영역을 구성한다.
- `src/views/DashboardView.vue` 43-47행에서 Chart.js와 데이터 유틸을 사용한다.
- `src/views/DashboardView.vue` 67-112행에서 차트를 생성한다.

## 5. 최종 실습기획서 반영 검토

### 잘 반영된 부분

| 기획서 내용 | 프로젝트 반영 |
|---|---|
| 서울 권역 선정 | 서울 JSON 7개 파일 사용 |
| Vue 3 + Vite SPA | package, router, build 구성 반영 |
| Netlify 배포 | `netlify.toml` 구성 완료 |
| Netlify Functions 챗봇 | `netlify/functions/chat.mjs` 구현 |
| `OPENAI_API_KEY` 서버 환경변수 | Function에서만 사용 |
| `VITE_OPENAI_API_KEY` 금지 | 검색 결과 프론트 코드에 없음 |
| 데이터 시각화 대시보드 | DashboardView와 Chart.js 구현 |
| 데이터 출처/라이선스 문서화 | `서울/SOURCE.md`, README, docs에 반영 |
| WBS/역할 분담 | docs 하위 문서에 반영 |

### 조정이 필요한 표현

최종 실습기획서에는 `공통 UI`, `레이아웃`, `Header`, `Footer`가 Team A 업무로 비교적 크게 잡혀 있다. 팀 작업 분담 문서로는 괜찮지만, 의뢰서 요구사항 문서에서는 이를 필수 요구사항으로 보이게 쓰지 않는 편이 낫다.

권장 정리:

- 의뢰서 필수: Vue SPA, 제공 JSON, 익명 커뮤니티, 챗봇, Netlify 배포, 산출물
- 내부 구현 보조: Header, Footer, 공통 버튼, 반응형 레이아웃, 대시보드 polish
- 선택 기능: 데이터 시각화 대시보드

## 6. 이전 Markdown 대비 수정 방향

기존 `LocalHub_사이트_화면기능_요구사항.md`는 사이트 전체 화면 설계를 넓게 잡는 데는 유용하지만, 사용자가 강조한 의뢰서 요청 내용만 기준으로 보면 아래 항목이 과해 보일 수 있다.

| 기존 문서 항목 | 검토 결과 | 수정 방향 |
|---|---|---|
| 공통 레이아웃을 모든 주요 화면 기준으로 상세 정의 | 의뢰서 직접 필수 아님 | 최소 내비게이션/공통 구조 정도로 축소 |
| 지역 정보 상세 화면을 필수처럼 작성 | 의뢰서의 직접 문구는 데이터 활용과 커뮤니티 중심 | 있으면 좋지만 MVP에서는 목록/검색 중심도 가능 |
| 지도 보기 버튼, 상세 필드, 장소 복사 등 | 선택 기능 성격 | 선택/추가 개선으로 분리 |
| 게시판 목록/상세/작성 화면 완전 분리 | 와이어프레임 참고 수준 | 평가 기준에 따라 권장, 필수 기능은 CRUD |
| 음식점/맛집을 카테고리 필수처럼 표현 | 현재 제공 JSON에 음식점 없음 | 제공 데이터에 있는 카테고리만 기준 |
| 모바일 전체 UX 세부 기준 | 품질 기준으로는 좋지만 의뢰 핵심은 아님 | 완료 기준의 보조 항목으로 이동 |

## 7. 현재 프로젝트의 핵심 충족도

| 요구 범주 | 충족도 | 판단 |
|---|---:|---|
| 의뢰서 요청 내용 | 85% | 핵심 구조와 기능은 반영됨 |
| 최종 실습기획서 | 80% | Netlify Functions, 대시보드, 데이터 문서화 반영 |
| 참고 와이어프레임 일치 | 55% | 커뮤니티 화면 분리와 테이블/페이지네이션은 부족 |
| 발표 가능성 | 80% | 빌드/테스트 통과, 핵심 시연 가능 |
| 보안 방향 | 85% | 프론트 키 노출 방지 구조 반영 |

## 8. 주요 이슈 및 리스크

### 8.1 커뮤니티 화면 구조

현재 커뮤니티는 기능을 한 화면에 모아둔 MVP 형태다. 의뢰서 참고 이미지처럼 목록, 상세, 작성/수정 화면을 분리하지 않았다.

판단:

- 필수 기능 관점: 허용 가능
- 와이어프레임 충실도 관점: 부분 미흡

### 8.2 게시판 검색과 페이지네이션

의뢰서 참고 이미지에는 게시글 검색과 페이지네이션이 보인다. 현재 지역 정보 목록에는 검색과 더보기가 있지만, 커뮤니티 게시글 목록에는 검색/페이지네이션이 없다.

판단:

- 의뢰서 "요청 내용" 문단만 보면 필수라고 단정하기 어렵다.
- 참고 화면까지 평가 기준으로 삼으면 개선 대상이다.

### 8.3 데이터 건수 불일치

현재 프로젝트 JSON 파일 합산은 6,518건이다. 일부 문서에는 8,150건이 언급된다.

판단:

- 발표자료와 기능 명세서에는 "현재 제공 파일 기준 6,518건"으로 맞추는 것이 안전하다.
- 원본 수집 총량과 실제 앱 포함 파일 수가 다를 수 있다고 설명한다.

### 8.4 작업트리 상태

검토 시점의 `git status --short` 결과, 다음 파일이 이미 수정 상태로 표시되었다.

```text
 M .gitignore
 M src/assets/tailwind.css
 M src/components/Chatbot.vue
```

본 검토 작업에서는 해당 파일들을 수정하지 않았다. 새로 작성한 산출물 MD만 추가했다.

## 9. 최종 판단

현재 프로젝트는 의뢰서의 핵심 요청인 "서울 권역 제공 JSON 기반 Vue 3 정적 SPA", "익명 커뮤니티", "localStorage 기반 게시글", "챗봇", "Netlify 배포 구조"를 대체로 잘 반영하고 있다.

다만 문서화 관점에서는 공통 레이아웃을 필수 요구사항처럼 강조하지 않는 것이 더 정확하다. 공통 레이아웃은 내부 구현 구조 또는 발표 품질 개선 항목으로 낮추고, 요구사항 본문은 다음처럼 좁히는 것이 좋다.

최종 요구사항 표현:

> LocalHub는 제공된 서울 JSON 데이터를 Vue 3 SPA에서 직접 불러와 지역 정보를 탐색할 수 있게 하고, localStorage 기반 익명 커뮤니티 CRUD와 Netlify Functions 기반 챗봇을 제공하는 정적 배포형 MVP이다. 공통 레이아웃은 화면 이동과 사용자 편의를 위한 구현 보조 요소이며, 의뢰서의 핵심 필수 범위는 데이터 활용, 익명 커뮤니티, 챗봇, Netlify 배포, 산출물 제출이다.

## 10. 권장 산출물 문서 구조

새 요구사항 MD를 다시 만든다면 아래 정도가 적정하다.

1. 의뢰서 요청 내용 요약
2. 최종 구현 범위
3. 필수 기능
   - 서울 JSON 데이터 조회
   - 익명 커뮤니티 CRUD
   - 챗봇
   - Netlify 배포
4. 선택 기능
   - 데이터 시각화 대시보드
5. 제외 범위
   - 회원/로그인
   - 서버 DB
   - WebSocket
   - 지도/날씨/다국어 등 미선택 기능
6. 데이터 및 라이선스
7. 챗봇 보안 구조
8. 현재 프로젝트 반영 여부
9. 남은 리스크
10. 완료 기준

